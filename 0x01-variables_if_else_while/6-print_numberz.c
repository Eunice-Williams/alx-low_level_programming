#include <stdio.h>
/**
 * main - A program that prints numbers in base 10 using putchar()
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int digits;

	while (digit < 10)
	{
		putchar((digits % 10) + '0');
		digit++;
	}
	putchar("\n");
	return (0);
}

