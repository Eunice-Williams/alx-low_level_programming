#include <stdio.h>
/**
 * main - A program that prints numbers in base 10 using putchar()
 *
 * Return: 0 always (success)
 */
int main(void)
{
	int digits;

	while (digit < 10)
	{
		putchar(digits);
		digit++;
	}
	putchar("\n");
	return (0);
}

